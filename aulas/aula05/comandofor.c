#include <stdio.h>

int main() {
  int numero = 0;
  
  printf("entre com um número de 1 a 10: ");
  int leu_certo = scanf("%i", &numero);
  
  for(int i = 0; i < 10; i++) {
    printf("%i x %i = %i\n", numero, i+1, numero * (i+1));
  }
for (;;){
  int opcao = 0;
  orintf("entre com uma opcao: ");
  leu_certo = scanf("%i", &opcao);
  if()
}

  return 0;
}