//Faça um programa em C que leia dois números inteiros e imprima o quociente e o resto da divisão entre eles.

#include <stdio.h>

int main(){
 float base = 8;
  float altura = 4;
  float area_t = base*altura / 2;
  printf("area do triangule é %f\n", area_t);


  return 0;
}